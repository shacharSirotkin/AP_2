<Window x:Class="GUI.AreYouSure"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI"
        mc:Ignorable="d"
        Height="170" Width="300">
    <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,0,0,-28">
        <TextBlock TextWrapping="Wrap" HorizontalAlignment="Center" Text="Are you sure?" FontSize="20" Margin="71,20,70,40"/>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="OKButton" Content="Yes" Width="90" Grid.Column="0" Click="OKButton_Click"/>
            <Button x:Name="CancelButton" Content="No, Go back!" Width="90" Grid.Column="1" Click="CancelButton_Click"/>
        </Grid>
    </StackPanel>
</Window>
