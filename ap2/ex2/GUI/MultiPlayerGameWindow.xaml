<Window x:Class="GUI.MultiPlayerGameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI"
        xmlns:controls="clr-namespace:GUI.Controls"
        mc:Ignorable="d"
        Height="450" Width="600">
    <StackPanel x:Name="stackPanel" Orientation="Vertical">
        <Button Content="Back To Main Menu" Height="40" Width="200" FontSize="16" HorizontalAlignment="Center" Margin="0,10,0,0" Click="Button_Click"/>
        <Grid Name="Grid">
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Your Board" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Column="0"/>
            <TextBlock Text="Other player's Board" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Column="1"/>
        </Grid>
    </StackPanel>
</Window>